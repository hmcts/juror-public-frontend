;(function(){
  'use strict';

  // Production specific configuration
  // =================================
  module.exports = {
    // If true, logs will be output in terminal as well as saved to file
    logConsole: false,
  };

})();
